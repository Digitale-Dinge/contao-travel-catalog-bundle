<h1>List</h1>

{% for travel in travels %}
    <h3>{{ travel.name }} ({{ travel.travel_code }})</h3>
    <a href="{{ travel.href }}">{{ travel.href }}</a>
    {{ dump(travel) }}
{% else %}
    Keine Reisen gefunden.
{% endfor %}

{{ pagination|raw }}